<p id="notice"><%= notice %></p>

<p>
  <strong>Course:</strong>
  <%= @course.name %>
  <%= @course.description %>
  <%= @course.price %>
</p>

<%= link_to '编辑', edit_course_path(@course) %> |
<%= link_to '返回', courses_path %>
